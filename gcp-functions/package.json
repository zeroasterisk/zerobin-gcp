{
  "name": "zerobin-gcp",
  "version": "0.0.1-alpha1",
  "dependencies": {
    "@google-cloud/firestore": "0.17.0",
    "semver": "^5.5.1"
  },
  "scripts": {
    "deploy": "gcloud functions deploy main --trigger-http",
    "stage": "functions deploy main --trigger-http",
    "lint": "semistandard --fix",
    "test": "ava"
  },
  "devDependencies": {
    "ava": "1.0.0-beta.8",
    "body-parser": "^1.18.3",
    "express": "^4.16.3",
    "semistandard": "^12.0.1",
    "supertest": "^3.3.0"
  }
}
